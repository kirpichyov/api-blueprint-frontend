const AccessToken = 'accessToken';
const RefreshToken = 'refreshToken';
const AccessTokenExpiresAtUtc = 'accessTokenExpiresUnix';
const RefreshTokenExpiresAtUtc = 'refreshTokenExpiresUnix';
const UserId = 'userId';

export default {
    AccessToken,
    AccessTokenExpiresAtUtc,
    RefreshToken,
    RefreshTokenExpiresAtUtc,
    UserId,
};